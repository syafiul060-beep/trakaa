# Railpack/Nixpacks config - pastikan build dari traka-api
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
# Ganti flag legacy `--production` / `config production` (npm 9+).
cmds = ["npm ci --omit=dev"]

[phases.build]
cmds = []

[start]
cmd = "node src/index.js"

# Railway config untuk traka-api
# Pastikan Root Directory = traka-api di Settings > Source

[build]
builder = "NIXPACKS"

[deploy]
# Langsung `node` — hindari CLI npm saat start (noise: npm warn config production).
startCommand = "node src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 60
